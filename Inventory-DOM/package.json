{
  "name": "js-dom",
  "version": "1.0.0",
  "description": "* start from the up-to-date master branch (`git checkout master; git pull origin master`)\r * Create a feature branch named 'js-dom' (`git checkout -b js-dom`)\r * add/modify the files in this directory to have the required features\r * add, commit, and push the branch to github\r * Create a PR to merge to master\r * Be sure to include the TA(s) and I as reviewers.  \r * Due by 11:59pm (PT) Sun Jun 21",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
